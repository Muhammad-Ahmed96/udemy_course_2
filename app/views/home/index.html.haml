%h1 Home#index
%p Find me in app/views/home/index.html.erb

- if user_signed_in?
  = link_to("Log Out",destroy_user_session_path, method: :delete)
- else
  = link_to("Log In",new_user_session_path)
= link_to "Blog", blogs_path
=link_to "Portfolio", protfolios_path
